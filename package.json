{
  "private": true,
  "name": "litbeam-monorepo-root",
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix LitBeam install && npm --prefix LitBeam run build",
    "dev": "npm --prefix LitBeam run dev"
  }
}
